Back-End project for the insurance company Carlib.
Uses the Spring Data JPA framework to communicate with a relational database and perform different CRUD operations: create, read, modify and delete data.
